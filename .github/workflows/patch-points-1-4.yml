name: One-time Point 2 PDF Patch

on:
  push:
    paths:
      - ".github/workflows/patch-points-1-4.yml"
      - ".github/scripts/patch_point_2_pdf.py"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply PDF patch
        run: python .github/scripts/patch_point_2_pdf.py
      - name: Commit App changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx
          if git diff --cached --quiet; then
            echo "No changes."
            exit 0
          fi
          git commit -m "Fix rebag PDF reconciliation and stock card units"
          git push
